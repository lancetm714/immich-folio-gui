# Immich Server Connection
IMMICH_API_URL=http://your-immich-server:2283
IMMICH_API_KEY=your-api-key-here

# Optional: Site title and subtitle (default: "Gallery")
SITE_TITLE=My Photography
SITE_SUBTITLE=A visual journal

# Optional: Cache TTL in seconds (default: 300 = 5 minutes)
CACHE_TTL=300

# Optional: Rate limit for image proxy (requests per minute per IP, default: 120)
# RATE_LIMIT_RPM=120

# ── Album configuration ──
# Albums, subpages, hero image, and passwords are defined in content/gallery.yaml
# See content/gallery.yaml.example for the full reference.
